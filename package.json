{
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "build": "worktop build src/index.ts",
        "dev": "miniflare -w",
        "deploy": "wrangler publish"
    },
    "devDependencies": {
        "miniflare": "^2.13.0",
        "typescript": "^5.0.2",
        "worktop.build": "^0.0.5",
        "wrangler": "^2.13.0"
    },
    "dependencies": {
        "worktop": "0.8.0-next.14"
    }
}
